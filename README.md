A simple static page built using React. Showcases a range of travel locations for your inner wanderlust. 

- Uses a component that accepts props for a destination
- Created an array of destinations which is mapped over the component
